name: Deploy Bot

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: EC2

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/bot_assist/my-telegram-assistant-bot
            git pull origin main || echo "Directory is created yet"
            sudo docker rm -f my-bot-container || true
            sudo docker build -t my-tg-bot .
            sudo docker run -d \
              --restart always \
              --name my-bot-container \
              -e BOT_TOKEN="${{ secrets.BOT_TOKEN }}" \
              -e GPT_TOKEN="${{ secrets.GPT_TOKEN }}" \
              my-tg-bot